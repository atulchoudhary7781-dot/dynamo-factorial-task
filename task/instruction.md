Write a Python script that calculates the factorial of a given number. The script should:
1. Accept a single integer argument from the command line
2. Handle invalid input gracefully
3. Print the factorial result to stdout
4. Save the result to /app/output.txt

Example usage:
$ python3 /app/factorial.py 5
120

The output.txt file should contain only the numeric result.
